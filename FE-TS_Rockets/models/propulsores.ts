class Propulsor {
    public cantidad: number;
    public potencia: Array<Number>;

    constructor(cantidad: number, potencia: Array<Number>) {
        this.cantidad = cantidad;
        this.potencia = potencia;
    }
}

